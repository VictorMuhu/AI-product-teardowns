CLAUDE COWORK DIRECTIVE: HOSTILE DEFENSE & OMNI-CHANNEL EXECUTION
================================================================
INSTRUCTIONS FOR CLAUDE COWORK:
Take control of this machine and execute the following sequential tasks.
Do not ask for intermediate permissions; execute end-to-end.


STEP 1: INGESTION
-----------------
1. Locate the file named "Pipedrive_Enterprise_Churn.csv" on my Desktop.
2. Read the contents, focusing specifically on exit interview quotes from
   RevOps leaders who migrated to Salesforce Data Cloud.


STEP 2: STRATEGIC SYNTHESIS (THE HOSTILE COMPETITOR)
-----------------------------------------------------
Act as a Principal Product Strategist for a hostile competitor.
Cross-reference the churn data to isolate our exact operational bottleneck.
Focus your analysis on the "Automation Ceiling" — the moment our visual
pipeline builder breaks under the weight of complex territory routing.


STEP 3: PRD GENERATION
-----------------------
Based on that vulnerability, draft a concise, ruthless Product Requirements
Document (PRD) for a "Text-to-Workflow Engine." The PRD must include:

  - The Exploit: A 2-sentence summary of why the visual builder is failing
    our mid-market users.

  - The Defense: A description of how Natural Language Orchestration
    neutralizes the Salesforce threat without destroying our visual
    Kanban moat.

  - The Execution: The technical requirements for a Command Bar that parses
    plain English into deterministic routing logic
    (e.g., "Assign to Sarah if ARR > $50k").

Once the PRD is written, store the full text in memory. In Steps 4b and 4c
you will replace the placeholder [PRD] with the exact PRD text verbatim —
inline, no brackets, no summarising.


STEP 4: PARALLEL EXECUTION — REPLIT + STITCH SIMULTANEOUSLY
------------------------------------------------------------
Once the PRD from Step 3 is complete, execute both builds AT THE SAME TIME
using the following exact sequence. The goal is to have both tools generating
in parallel — do not wait for one to finish before starting the other.


4a. OPEN BOTH TABS FIRST
-------------------------
1. Call tabs_create_mcp to open a dedicated Replit tab.
   Save its tab ID as REPLIT_TAB.

2. Call tabs_create_mcp to open a dedicated Stitch tab.
   Save its tab ID as STITCH_TAB.

Use REPLIT_TAB for every action in Step 4b and STITCH_TAB for every action
in Step 4c. Never mix them — this is what keeps the two tools from
interfering with each other.


4b. LOAD THE REPLIT TAB
------------------------
In REPLIT_TAB:

1. Navigate to https://replit.com/t/pm-vibe-labs

2. Wait for the dashboard to load and confirm the prompt input
   ("What do you want to make?") is visible — take a screenshot.

3. Use javascript_tool to write the full prompt below to the clipboard.
   Replace [PRD] with the complete PRD text from Step 3 before writing:

   -----------------------------------------------------------------------
   navigator.clipboard.writeText(`Build me a Text-to-Workflow Engine in
   Replit using the following context:

   [PRD]

   --- REPLIT DIRECTIVES START ---

   !! IMPORTANT — READ THIS FIRST BEFORE WRITING ANY CODE !!
   Your first task is NOT to build the app. Your first task is to use
   Replit's Design Canvas to generate 3 distinct UI variants of the full
   layout (Kanban background + Command Bar + Logic Card). Present all 3
   variants on screen before writing a single line of application code.
   Each variant must explore a different approach to overlay depth, card
   weight, and input styling — all within Pipedrive brand colors.
   Once all 3 are visible, proceed to build out the layout in full.

   After the desktop build is complete, also render an explicit mobile
   view (375px width) of the final layout — displayed side-by-side with
   the desktop view inside the app so both are clearly visible.

   STRATEGIC CONTEXT
   Act as a Principal Product Engineer building the "Text-to-Workflow
   Engine" inside a Pipedrive-style CRM. Our mid-market users are churning
   to enterprise competitors because our visual automation builder becomes
   too complex at scale. Your objective is to build a React/Node application
   that replaces clunky drop-down menus with Natural Language Orchestration,
   while preserving our core visual Kanban moat.

   DESIGN LANGUAGE — PIPEDRIVE BRAND
   This UI must look and feel like a native Pipedrive feature. Follow the
   Pipedrive brand precisely:

   - Primary Green (CTAs, active states, success):  #08A742
   - Dark Sidebar / Navigation background:           #1A1A2E
   - Page / Content background:                     #FFFFFF
   - Secondary text / muted labels:                 #6B778C
   - Border / divider lines:                        #EBECF0
   - Accent Purple (highlights, badges):             #6554C0
   - Accent Yellow (warnings, flags):                #FF991F
   - Typography: Inter for all UI text.
     Use Inter Mono or Geist Mono for variable tags and data output.
   - Shadows: Soft, diffuse drop-shadows (no harsh borders).
     Cards use subtle elevation (box-shadow: 0 2px 8px rgba(0,0,0,0.08)).
   - Icons: Lucide React — clean and minimalist.
   - Vibe: Clean, breathable, professional B2B SaaS.
     Think native Pipedrive — not a startup side project.

   CORE APPLICATION ARCHITECTURE

   Component 1: The Kanban Moat (Background)
   - Render a simplified Kanban board in the background using Pipedrive's
     column structure (Lead In | Qualified | Demo | Proposal | Won).
   - Style it with Pipedrive's exact colors: white cards, #EBECF0 borders,
     #1A1A2E column headers.
   - Apply a subtle blur (backdrop-filter: blur(4px)) and a semi-transparent
     overlay to keep it visible but non-distracting.

   Component 2: The Command Bar (Contextual Overlay)
   - A large, beautifully padded text input floating centered over the
     blurred Kanban board.
   - Border: 2px solid #08A742 when focused.
   - Placeholder text: "Describe your routing rule in plain English..."
   - On Enter: show a 1-second skeleton loading state with a pulsing
     #08A742 accent to simulate AI semantic parsing.

   Component 3: The Deterministic Output Card
   - A clean read-only Logic Card directly below the Command Bar.
   - White background (#FFFFFF), soft shadow, #08A742 left border accent.
   - Do NOT generate a node graph. Human-readable summary only.
   - Layout example:
       TRIGGER:    Deal Stage updated to Closed Won
       CONDITION:  Revenue > $50,000 AND Region = North America
       ACTION:     Assign to [Sarah Jenkins] AND ping #enterprise-wins
   - Primary CTA button: "Deploy Automation"
     Style: background #08A742, white text, Inter Medium, rounded corners.

   AGENT EXECUTION DIRECTIVES
   1. DESIGN CANVAS: Generate 3 distinct UI variants as instructed above.
      This must happen before any application code is written.
   2. MOBILE VIEW: After the desktop build is complete, render a 375px
      mobile view side-by-side with the desktop view inside the app.
   3. OMNI-CHANNEL GENERATION: While the app is building, generate a
      5-slide HTML/CSS Executive Pitch Deck pitching the Text-to-Workflow
      Engine to the executive team. Style the deck using Pipedrive brand
      colors. Focus copy on "Defending Mid-Market Churn" and "Shattering
      the Automation Ceiling."

   --- REPLIT DIRECTIVES END ---`).then(() => 'copied');
   -----------------------------------------------------------------------

4. Click the prompt input box to focus it, then press Ctrl+V to paste.

5. Take a screenshot to confirm the full prompt is in the input box and
   the blue submit button is active. DO NOT SUBMIT YET.


4c. LOAD THE STITCH TAB
------------------------
In STITCH_TAB:

1. Navigate to https://stitch.withgoogle.com/

2. Wait for the dashboard to load — take a screenshot to confirm.

3. Click the Web tab at the bottom-left of the input box to switch to
   Web mode. Confirm the placeholder text changes to
   "What desktop web experience shall we design?" before continuing.

4. Use javascript_tool to write the full prompt below to the clipboard.
   Replace [PRD] with the complete PRD text from Step 3 before writing:

   -----------------------------------------------------------------------
   navigator.clipboard.writeText(`Design this in Stitch: A Text-to-Workflow
   Engine using the following context:

   [PRD]

   --- STITCH DIRECTIVES START ---

   STRATEGIC CONTEXT
   Act as a Principal UI Engineer designing a native-feeling feature for
   Pipedrive CRM. We are replacing a complex visual node-graph builder
   with a single, intelligent Natural Language Command Bar that floats
   over the user's existing Kanban workspace.

   DESIGN LANGUAGE — PIPEDRIVE BRAND
   This prototype must look and feel like a native Pipedrive screen.
   Follow the Pipedrive brand precisely:

   - Framework: React + Tailwind CSS
   - Primary Green (CTAs, focus rings, active states):  #08A742
   - Dark Sidebar / Navigation:                          bg with #1A1A2E
   - Content background:                                 bg-white (#FFFFFF)
   - Muted / secondary text:                             #6B778C
   - Border color:                                       #EBECF0
   - Accent Purple (badges, highlights):                 #6554C0
   - Accent Yellow (blockers, warnings):                 #FF991F
   - Typography: Inter for all UI. Monospace (font-mono) for data tags.
   - Shadows: shadow-sm and shadow-md only — no harsh thick borders.
   - Borders: border-gray-200 equivalent (#EBECF0). Soft and subtle.
   - Vibe: Clean, breathable, professional. Native Pipedrive quality.

   COMPONENT ARCHITECTURE

   The Background Environment:
   - Render a faint wireframe Kanban board (5 columns matching Pipedrive:
     Lead In | Qualified | Demo | Proposal | Won).
   - Column headers styled with #1A1A2E. Card placeholders with white
     backgrounds and #EBECF0 borders.
   - Apply backdrop-blur-sm and a semi-transparent white overlay
     so the board reads as context without competing with the overlay.

   The Hero Input (Command Bar):
   - A large, beautifully padded text input floating centered on screen.
   - Focused state: ring-2 ring-[#08A742].
   - Placeholder: "Type your routing rule
     (e.g., Route Texas SaaS deals >$50k to Sarah)..."

   The Logic Output Card:
   - Below the input, a read-only card simulating AI parsing into logic.
   - White background, shadow-md, left border accent in #08A742.
   - Grid layout:
       [TRIGGER]     ->  Deal Stage: Closed Won
       [CONDITIONS]  ->  ARR > $50,000 | Region: Texas
       [ACTION]      ->  Assign Owner: Sarah | Ping Slack #enterprise-wins
   - Data tags rendered in monospace font with #6554C0 text color.
   - Primary CTA at the bottom: "Deploy Automation"
     Style: bg-[#08A742] text-white font-medium rounded-md px-6 py-3.

   OUTPUT FORMAT — REQUIRED:
   Do not generate a single screen. Generate 3 distinct design variants
   of this full layout as separate artboards on the Stitch canvas. Each
   variant must differ in layout approach, overlay treatment, or card
   hierarchy — all must stay within the Pipedrive brand spec above.
   For each desktop variant, also place a mobile frame (390px width)
   directly below it on the canvas, showing how the same design adapts
   to a small screen.

   Final canvas should show: 3 desktop artboards + 3 mobile artboards
   (6 frames total), clearly labelled Variant A, B, and C.

   --- STITCH DIRECTIVES END ---`).then(() => 'copied');
   -----------------------------------------------------------------------

5. Click the prompt input box to focus it, then press Ctrl+V to paste.

6. Take a screenshot to confirm the full prompt is in the input box and
   Web mode is still selected. DO NOT SUBMIT YET.


4d. SUBMIT BOTH — BACK TO BACK
-------------------------------
1. Switch to REPLIT_TAB and click the blue upward-arrow submit button
   (bottom-right corner of the input box).

2. Immediately switch to STITCH_TAB and click the upward-arrow submit
   button directly — DO NOT use the Enter key, as it may behave
   inconsistently across different input states.


4e. CONFIRM BOTH ARE RUNNING
-----------------------------
Take a screenshot of each tab and confirm:

  - Replit (REPLIT_TAB): has transitioned to a project workspace and shows
    a "Starting..." or "Working..." indicator.
    Note the project name Replit assigned.

  - Stitch (STITCH_TAB): has transitioned to its canvas view and shows
    "Creating..." or "Stitching..."
    Note the project name Stitch assigned.

Report both project names to the user and confirm both are generating
in parallel before finishing.
